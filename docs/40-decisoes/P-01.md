---
title: "P-1 — Chaves de host SSH duplicadas"
tags:
  - pendencia
severidade: Alta
status: aberta
data: 2026-08-10
---

# P-1 — Homolog e produção compartilham as mesmas chaves de host SSH

Homolog e produção têm as mesmas chaves de host SSH (`ssh-rsa`, `ecdsa`, `ed25519`)
idênticas — indício de VMs clonadas de um template com as chaves já geradas, em vez de cada
uma gerar as suas no primeiro boot. Enfraquece o pinning de `known_hosts`: uma chave
comprometida em um ambiente compromete o pinning do outro.

**Ação:** regenerar as chaves de host de cada VM individualmente (`ssh-keygen` +
reconfiguração do sshd), fora do escopo desta migração.
